apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dglm-data
  namespace: dglm
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: hcloud-volumes
  resources:
    requests:
      storage: 10Gi   # minimo Hetzner Cloud Volumes
